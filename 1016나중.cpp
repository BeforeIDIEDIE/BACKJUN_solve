#include<iostream>

using namespace std;

int main() 
{
    int min = 0, max = 0;
    cin >> min >> max;
    int res = max - min + 1, i = 2;
    int count = 0;
    while (i * i <= max) 
    {
        if()
    }
    cout << res-count;
}