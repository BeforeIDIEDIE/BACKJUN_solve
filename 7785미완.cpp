#include<iostream>
#include<set>
#include<algorithm>

using namespace std;

int main()
{
    // 입출력 스트림의 동기화를 끊음
    ios_base::sync_with_stdio(false);

    // cin과 cout의 동기화를 끊음
    cin.tie(NULL);

    set<string> set1;
    string arr[2];
    
    int a = 0;
    cin >> a;

    set.find
        /*
        * 
        */
    for (int i = 0; i < a; i++)
    {
        cin >> arr[0] >> arr[1];
        if(set1.find[])
    }

}